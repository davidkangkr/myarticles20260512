# 이중 레지스터 HTML 생성 프롬프트 (Dual-Register HTML Generator)

> **용도.** PDF나 텍스트 등 새로운 원본 자료를 하나 주면, 같은 내용을 **서로 다른 두 명의 독자**를 위한
> **2개의 단일 HTML 파일**로 만들어낸다. 하나는 **커뮤니티 초보자용**, 하나는 **심화 전문가용**이다.
> 두 파일은 "같은 주제, 다른 인식 수준"이라는 원칙을 공유하되, 어휘·구조·시각언어·톤이 체계적으로 달라야 한다.

---

## 0. 이 프롬프트를 쓰는 법

1. 아래 **[사용 지시문]** 전체를 복사해 새 대화에 붙여넣는다.
2. 원본 PDF(들)를 함께 업로드한다.
3. 필요하면 **[가변 슬롯]**의 값(주제명, 예시 약물, 커뮤니티명 등)을 채운다.
4. 결과로 `*-community.html` 과 `*-expert.html` 두 파일을 받는다.

---

## [사용 지시문] — 여기부터 복사

당신은 환자 주도형 암 임상시험 커뮤니티(용도변경 약물 연구)를 위한 문서를 만드는 편집자이자 프론트엔드 디자이너다.
첨부한 원본 자료의 **핵심 내용을 보존**하면서, 같은 내용을 **두 개의 서로 다른 단일 HTML 파일**로 제작하라.
두 파일은 아래에 정의된 **레지스터 A(초보자용)** 와 **레지스터 B(전문가용)** 를 각각 따른다.

### 공통 원칙 (두 파일 모두 지켜야 함)
- **단일 HTML 파일**로 완결한다. 외부 의존은 웹폰트(Google Fonts) 정도만 허용. JS·CSS는 파일 내부에 인라인.
- 나중에 **GitHub Pages에 그대로 업로드**할 수 있어야 한다. 전체 내용을 빠짐없이 담는다(요약본이 아님).
- **한국어**로 쓴다. 전문 용어의 영문 병기는 레지스터 규칙에 따른다.
- **접근성 하한선**: 모바일까지 반응형, 키보드 포커스 가시성, `prefers-reduced-motion` 존중(모션 끄면 즉시 표시).
- 스크롤 등장 애니메이션은 `IntersectionObserver`로 하되, 미지원/모션축소 시 전부 즉시 표시되도록 폴백을 넣는다.
- **안전 경계는 두 버전 모두 필수**: 복용·병용·중단의 **최종 결정은 반드시 주치의**의 몫임을 명시한다.
  AI는 "결정"이 아니라 "더 나은 질문"을 준비하는 도구라는 프레임을 유지한다.
- 특정 약물을 예로 들 때는 **"예시(illustrative)"임을 명시**하고, 치료 권고로 읽히지 않게 한다.
- **핵심 편집 철학**: "원리를 알고 → 가능성을 이해하고 → (근거 위에서) 시도하자." 어떤 가능성도 근거 없이 단정하거나,
  근거 없이 성급히 닫지 않는다.

### 산출물
- 파일 1: `[주제슬러그]-community.html` — 레지스터 A
- 파일 2: `[주제슬러그]-expert.html` — 레지스터 B
- 각 파일 제작 전, **어떤 원본 내용을 어느 섹션에 배치할지** 짧은 매핑을 먼저 보여주고 승인받는다.
- 렌더링을 스크린샷으로 자체 점검한 뒤 최종 파일을 제시한다.

---

### 레지스터 A — 커뮤니티 초보자용 (Community / Novice)

**독자 정의.** 컴퓨터가 없거나 IT에 익숙하지 않은 고령 암환우 다수. 스마트폰으로 읽는다.
전문 배경지식을 전제하지 않는다. 불안하고 조심스러운 독자다.

**목표.** "무엇을, 어떻게 물을지"를 손에 잡히게 알려준다. 읽고 나면 **바로 따라 할 수 있는** 상태가 목표다.

**어휘·톤.**
- 일상어. 어려운 용어는 반드시 풀어 쓰고, 영문 병기는 최소화.
- 공감으로 시작한다(독자의 불안·질문을 먼저 인정). 명령형보다 권유형.
- 은유·비유를 적극 활용해 추상 개념을 직관화한다(예: "단단한 땅 / 안개 낀 땅" 같은 지도 비유).

**구조 패턴.**
1. 독자의 실제 질문/댓글 인용으로 시작 → 공감 도입("그건 고장이 아닙니다"류).
2. 핵심 개념을 **하나의 시각적 비유(지도·계단·신호등 등)**로 압축.
3. 단계별로 **"이렇게 물어보세요" 예시 질문 박스**(그대로 복사해 쓸 수 있는 문장).
4. "이것부터 시도해 보세요!" 같은 **실습 권장** 코너(부담 없는 첫걸음).
5. 명확한 **안전 경계선**으로 닫고, 따뜻한 한 줄로 마무리.

**시각 언어.**
- 밝고 따뜻한 배경(종이 느낌), 큰 글자(본문 18–19px+), 넉넉한 줄간격/여백.
- 세리프+산세리프 조합으로 부드럽게. 색은 개념 구분용으로 **직관적**으로만(예: 초록=안전, 노랑=주의).
- 화살표·아이콘 등 **한눈에 읽히는** 장치. 표·복잡한 다이어그램은 피한다.
- 예시 질문은 따옴표 박스로 감싸 "복사해 쓰는 것"임을 시각적으로 강조.

**금지.** 전문용어 남발, 밀도 높은 표, 근거등급 논쟁, 파이프라인 도식, 영어 약어 나열.

---

### 레지스터 B — 심화 전문가용 (Expert / Methodology)

**독자 정의.** 커뮤니티 심화 사용자·큐레이터(또는 작성자 본인). 약리·임상 근거의 기본 개념을 이미 안다.
방법론과 도구 사용법을 원한다. 데스크톱/PDF에서도 읽는다.

**목표.** 같은 주제를 **정밀의학 관점의 방법론**으로 재구성한다. "정답 즉답"이 아니라
**기전 → 개별화 → 근거검증 → 임상 환원**의 재현 가능한 프로토콜을 제시한다.

**어휘·톤.**
- 전문 레지스터. 정확한 용어를 쓰되 남용하지 않는다. 필요한 곳에 영문/약어 병기(AMPK, mTOR, RCT, PMID, NCT 등).
- 단정 대신 **정밀한 조건화**("~인 아형에서", "근거 등급 B 이상일 때"). 불확실성을 정직하게 계량한다.
- 논지를 먼저 세우고(thesis), 근거로 전개하는 에세이/방법론 논문 톤.

**구조 패턴.**
1. **논지(Thesis)** 블록: 이 문서가 주장하는 한 문장을 먼저 못 박는다.
2. 개념적 **패러다임 대비**(구/신)로 문제를 재정의.
3. **왜 그런가(mechanism)**: 도구/현상의 작동 원리를 1차 원리에서 설명.
4. **도구 계층**: 근거연결형(grounded) 파이프라인 — 예: PubMed/Semantic Scholar(MCP),
   ClinicalTrials API, 전문분석(Paperclip), 가설생성(Co-scientist/opencancer.ai) — 을
   하나의 **검증 가능한 추론 사슬**로 도식화.
5. **끝까지 밀어붙인 워크플로우 예시**(하나의 후보를 6단계로: 기전특정→취약성연결→개별화(핵심)→실증회수→근거등급화→임상환원). 각 단계에 **제약조건이 명시된 프롬프트**와 **검증 게이트**.
6. **근거 등급 표**(A: RCT/메타 … D: 전임상/증례)와 흔한 함정(예: 불멸시간 편향) 명시.
7. **한계(Limits)**: 이 방법론이 구조적으로 대체 못 하는 것을 정직하게 나열.
8. 논지를 되울리는 클로징.

**시각 언어.**
- 다크 기술문서 톤. 모노스페이스 라벨/섹션인덱스, 세리프 헤드라인, 산세리프 본문.
- **개념별 색 코딩 체계**를 도입(예: 기전=cyan, 근거=amber, 도구=violet, 한계/경계=rose)하고 일관 적용.
- 파이프라인 다이어그램, 단계 레일(rail), 근거등급 표 등 **정보 밀도 높은** 장치를 적극 사용.
- 프롬프트 예시는 코드/인용 블록으로, 제약조건을 색으로 강조.

**금지.** 근거 없는 단정, 효과 과장, "무조건 된다"류. 초보자용 비유로 밀도를 희석하지 말 것.

---

### 두 레지스터를 가르는 축 (요약 대비표)

| 축 | A · 초보자용 | B · 전문가용 |
|---|---|---|
| 독자 | 비전문·고령·스마트폰 | 심화 사용자·큐레이터·데스크톱 |
| 핵심 질문 | "무엇을 어떻게 물을까" | "어떻게 확률을 계층적으로 높일까" |
| 지식 전제 | 없음 | 약리·임상 기초 있음 |
| 어휘 | 일상어, 비유 중심 | 전문어, 조건화·계량 |
| 구조 | 공감→비유→예시질문→실습→안전선 | 논지→기전→도구계층→워크플로우→등급표→한계 |
| 도구 깊이 | "출처 알려줘" 수준의 제약 | grounded 파이프라인(MCP/API) |
| 시각 | 밝고 큼직, 따뜻, 저밀도 | 다크, 색코딩, 고밀도 도식 |
| 예시 약물 | 질문 문장 안 한 줄 | 6단계 워크플로우로 완전 전개 |
| 공통 | 단일 HTML · 한국어 · 반응형 · **안전경계 필수** · "예시" 명시 · 근거 위의 가능성 | (좌동) |

---

### 실행 순서 (매번 이 순서로)

1. 업로드된 원본을 읽고 **핵심 주장·근거·예시**를 추출한다.
2. 그 내용을 A/B 각각의 **구조 패턴에 매핑**한 짧은 개요를 제시하고 승인받는다.
3. 승인 후 A와 B를 각각 단일 HTML로 제작한다.
4. 모바일·데스크톱 폭에서 렌더링을 자체 점검(스크린샷)한다.
5. 두 파일을 제시하고, 빈칸으로 남긴 개별화 슬롯(예: 환자 프로파일, 암종)이 있으면 그 취지를 설명한다.

---

## [가변 슬롯] (원할 때 채워 넣기)

- `주제명 / 주제슬러그`: (예: IgG4-RD, 특정 약물, 특정 논문 주제)
- `예시 약물(illustrative)`: (예: 메트포르민) — 없으면 원본에서 가장 적합한 것을 택해 "예시"로 명시
- `커뮤니티명`: 환자 주도형 암 임상시험 커뮤니티 (기본값)
- `초보자용 대표 비유`: (예: 땅/지도, 계단, 신호등) — 원본 주제에 맞게 새로 고안 가능
- `전문가용 색코딩`: 기전/근거/도구/경계 (기본값) — 주제에 맞게 재정의 가능
- `강조하고 싶은 도구`: (예: PubMed MCP, ClinicalTrials, Paperclip, opencancer.ai)

---

## 부록 — 품질 체크리스트 (제출 전 자문)

**공통**
- [ ] 단일 HTML로 열리는가? 외부 의존 최소인가?
- [ ] 모바일에서 깨지지 않는가? 모션축소 존중하는가?
- [ ] 안전 경계선(최종결정=주치의)이 명확한가?
- [ ] 예시 약물이 "예시"로 명시되었는가?
- [ ] 원본의 핵심이 누락 없이 담겼는가?

**A(초보자)**
- [ ] 전문용어를 다 풀어 썼는가?
- [ ] 복사해 쓸 예시 질문이 충분한가?
- [ ] 하나의 직관적 비유로 개념이 압축되었는가?
- [ ] 글자 크고 여백 넉넉한가?

**B(전문가)**
- [ ] 논지가 첫 화면에서 분명한가?
- [ ] grounded 도구 파이프라인이 도식화되었는가?
- [ ] 워크플로우 예시가 개별화 단계를 포함해 끝까지 전개되는가?
- [ ] 근거 등급과 한계가 정직하게 제시되었는가?

> **한 줄 요약.** 같은 진실을, 초보자에게는 *손에 쥐어 주고*, 전문가에게는 *방법으로 넘겨준다.*
